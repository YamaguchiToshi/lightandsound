# 音と光のインタラクティブ環境 --- 暫定仕様 v0.1

## 1. 目的

重度・重複障害のある子どもを主な対象として、**身体の動きやポインティングデバイスの操作に対し、大きな視覚・聴覚フィードバックを返す
Cause & Effect 型の学習環境**を構築する。

基本的な体験は、

**「動かす → 光が流れる → その場所から音が聞こえる」**

という単純で理解しやすい因果関係とする。

iPadをセンサー／入力装置／描画・音響処理装置として利用し、映像はプロジェクタで壁面へ大きく投影する。

------------------------------------------------------------------------

## 2. 想定システム

``` text
        ┌─ iPadの傾き
        │
        ├─ タッチ
        │
        ├─ ポインティングデバイス
        │    ├ マウス
        │    ├ トラックボール
        │    └ HIDジョイスティック等
        │
        └─ 将来的にはスイッチ等
                    │
                    ▼
            ┌───────────────┐
            │ Virtual Gravity │
            │   X : -1 ～ +1 │
            │   Y : -1 ～ +1 │
            └───────┬───────┘
                    │
             ┌──────┴──────┐
             ▼             ▼
        光粒子システム     音響システム
             │             │
             ▼             ▼
        プロジェクタ     ステレオ音響
             │             │
             └──────┬──────┘
                    ▼
               子どもへの
           視覚・聴覚フィードバック
```

入力方法の違いをアプリ本体から切り離し、すべてを **Virtual
Gravity（仮想重力）** という共通の2次元入力へ変換する。

------------------------------------------------------------------------

## 3. 動作環境

  項目             仕様
  ---------------- ----------------------------------
  端末             iPad
  ブラウザ         Safari
  実装             HTML / CSS / JavaScript
  外部ライブラリ   原則不要
  描画             HTML Canvas 2D
  センサー         DeviceMotion / DeviceOrientation
  音響             Web Audio API
  ポインター       Pointer Events
  映像出力         プロジェクタへのミラーリング
  音響             ステレオ
  ネット接続       原則不要とする方向

可能であれば最終的に **単一HTMLファイルでも動作可能** な構成を目指す。

------------------------------------------------------------------------

## 4. 基本となる体験

画面には多数の「光の粒」が存在する。

iPadが水平、またはVirtual
Gravityがほぼゼロの場合、粒はその場で静かに漂ったり、わずかに輝いたりする。

入力すると、その方向に重力が発生する。

たとえば右方向なら、**光の粒が右へザザーッと流れる。**

同時に、それぞれの粒から「キラッ」「リン」「シャラ」といった短い音が発生する。

光が右へ移動すれば音も右へ、左へ移動すれば音も左へ定位する。

------------------------------------------------------------------------

## 5. 入力システム

### 5.1 Virtual Gravity

内部的には入力を、

``` text
gravityX = -1.0 ～ +1.0
gravityY = -1.0 ～ +1.0
```

程度の共通値として扱う。

粒子システムは **何が gravityX / gravityY を生成したのかを意識しない**
設計とする。

これにより、子どもの身体状況に応じて入力方法だけを交換できる。

### 5.2 iPad傾斜モード

iPadの加速度・傾きを取得し、Virtual Gravityへ変換する。

``` text
iPadを右へ傾ける
       ↓
gravityX +
       ↓
光が右へ流れる
```

実際の傾斜角を粒の位置へ直接変換するのではなく、**傾き＝重力**として扱う。

そのため、粒には加速・減速・慣性が発生する。

### 5.3 ポインティングデバイスモード

iPadに接続されたポインティングデバイスから入力する。

想定するものには、

-   マウス
-   トラックボール
-   タッチパッド
-   マウスエミュレーションを行うジョイスティック
-   その他iPadでポインターとして認識される支援機器

などがある。

#### 基本方式

画面中央をニュートラルとする。

``` text
                 上
                 ↑
                 │
                 │
       左 ←───── ● ─────→ 右
                 │
                 │
                 ↓
                 下
```

中央からの **方向 → 重力方向**、**距離 → 重力の強さ** として扱う。

GUIのボタンを正確にポイントしたりクリックしたりする必要はない。

**ポインターを動かすこと自体を意味のある入力とする。**

### 5.4 将来的な入力方式

Virtual Gravityを介することで、

-   タッチ／ドラッグ
-   相対ポインター移動
-   4方向入力
-   1〜複数スイッチ
-   視線入力
-   その他の支援機器

などへの拡張を可能とする。

------------------------------------------------------------------------

## 6. 光粒子システム

### 6.1 基本モデル

各粒は最低限、

``` text
x
y
vx
vy
size
brightness
```

などの状態を持つ。

Virtual Gravityから加速度を与え、

``` text
重力
 ↓
加速度
 ↓
速度
 ↓
位置
```

として計算する。

適度な摩擦・減衰を設定し、入力停止後も少し慣性が残るようにする。

### 6.2 光の表現

壁面投影を前提とする。

#### 背景

**黒または極めて暗い色。**

装飾的な背景は原則使用しない。

#### 粒

高輝度の以下の色などを基本候補とする。

-   白
-   シアン
-   青
-   紫
-   黄

粒には、**明るい中心 + 周囲のGlow + ときどき発生するSparkle**
を持たせる。

### 6.3 移動速度による表現変化

低速時は「キラ......キラ......」と一つ一つの粒が認識できる。

高速時は「ザァーッ」と粒の移動方向へ残像が伸び、「光の川」のように見える。

したがって、**運動量 → 視覚的な変化量** が明瞭になるようにする。

### 6.4 画面端

基本的には粒が端に溜まる挙動を検討する。

反対方向へ入力すると、集まっていた粒が一斉に壁面を横切る。

これにより、小さな操作から **大きな視覚変化を蓄積して発生させる**
ことができる。

------------------------------------------------------------------------

## 7. 音響システム

Web Audio APIを使用する。

すべての描画粒を常時発音させる必要はなく、移動している粒の一部を
**「音粒」** として確率的に発音させる。

基本音は、

-   キラッ
-   リン
-   チン
-   シャラ

といった短く明瞭な音を想定する。

### 7.1 左右定位

最重要の対応関係とする。

``` text
画面左端   → pan -1.0
画面中央   → pan  0.0
画面右端   → pan +1.0
```

**視覚的位置と聴覚的位置を同期させる。**

### 7.2 その他の音響パラメータ

初期案として、

-   X位置 → 左右定位
-   Y位置 → 音程
-   移動速度 → 音量
-   全体の運動量 → 発音密度

という対応を検討する。

静かな動きでは「キラ......
リン......」、大きく動かすと「シャララララ......」という変化を作る。

ただし、刺激が過剰にならないよう、それぞれ独立して調整可能とすることが望ましい。

------------------------------------------------------------------------

## 8. 映像投影

iPadの画面をプロジェクタへ出力し、壁面へ大きく投影する。

重要なのは、**iPad画面上での美しさより、数m程度の壁面に投影した際の明瞭さ**
を優先することである。

そのため、

-   高コントラスト
-   大きめの粒
-   黒背景
-   明瞭な移動
-   過剰な細部を避ける
-   操作による変化量を大きくする

ことを基本的なデザイン原則とする。

------------------------------------------------------------------------

## 9. UI

遊んでいる最中には、通常のGUIを極力表示しない。

起動時に **「タッチしてスタート」** のような大きな開始操作を設ける。

この操作によってSafariで必要となる、

-   センサー利用開始
-   AudioContext開始

などを行う。

設定UIは遊びの画面とは分離する。

------------------------------------------------------------------------

## 10. 学習環境としての基本原則

このアプリでは、一般的なGUI操作の上手さを要求しない。

重要なのは、

> **子どもが現在可能な随意運動を入力として拾い、その結果を十分に知覚できる大きさの光と音に変換すること**

とする。

``` text
身体運動
   ↓
入力デバイス
   ↓
Virtual Gravity
   ↓
┌──────────┬──────────┐
↓                     ↓
光の方向・速度         音の方向・密度
↓                     ↓
└──────────┬──────────┘
           ↓
   大きな感覚フィードバック
```

特に **「入力方法」と「フィードバック」を分離する**
ことで、iPadを自力で傾けられる子どもと、トラックボール等で小さな随意運動を利用する子どもが、同じ光と音の環境で遊べるようにする。

------------------------------------------------------------------------

## 11. v0.1プロトタイプの範囲

最初の実装では機能を絞り、

**iPad傾斜 + ポインター入力 → Virtual Gravity → 光粒子 → ステレオ音響**

までを実装する。

実機を使って確認すべきなのは、技術的な動作以上に、**「どの程度の動きで、どの程度大きな結果を返すと気持ちよいか」**
である。

特に重度・重複障害のある子どもへの適用を考え、以下は固定値にせず、後から調整できるパラメータとして実装する。

-   傾斜感度
-   慣性
-   粒数
-   粒径
-   残像時間
-   音量
-   発音密度
-   音程幅
